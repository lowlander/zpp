tests:
  zpp.sample.kernel.cond_var:
    arch_exclude: posix
    platform_exclude: qemu_x86_coverage
    tags: zpp kernel condition_variables
    harness: console
    harness_config:
      type: one_line
      regex:
        - ".*Waited and joined with 3 threads"
